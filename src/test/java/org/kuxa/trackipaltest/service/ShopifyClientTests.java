package org.kuxa.trackipaltest.service;

import static org.mockito.Mockito.*;

import org.junit.jupiter.api.Test;
import org.springframework.web.client.RestTemplate;

class ShopifyClientTests {
    @Test
    void testGetAllOrders() {
//        RestTemplate mockTemplate = mock(RestTemplate.class);
//        ShopifyClient client = new ShopifyClient(mockTemplate, "fake-api-key", "https://fake-url/");
//
//        when(mockTemplate.getForObject(anyString(), eq(String.class))).thenReturn("response");
//
//        assert "response".equals(client.getAllOrders());
    }
}